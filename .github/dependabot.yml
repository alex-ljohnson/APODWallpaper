version: 2
updates:
  # Keep GitHub Actions (incl. SHA-pinned third-party actions) up to date.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
